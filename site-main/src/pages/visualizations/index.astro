---
// The visualizations listing moved under /projects when they were grouped behind a
// single card. Kept so older links keep working.
export const prerender = false

return Astro.redirect('/projects/visualizations', 301)
---
